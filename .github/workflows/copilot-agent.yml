name: Copilot Coding Agent

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  agent:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run agent
        run: |
          echo "Agents MUST follow AGENT OPERATING AGREEMENT in /AGENT_GUIDE.md"
          # Agent logic lives here; no auto-PRs, no auto-branching without explicit instructions.
