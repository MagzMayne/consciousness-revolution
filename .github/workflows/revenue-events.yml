name: Revenue Events

on:
  repository_dispatch:
    types: [sale-recorded]

permissions:
  contents: read

jobs:
  handle-sale:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Process sale
        run: |
          echo "Processing sale event: ${{ github.event.client_payload.id }}"
          # Call backend, update ledger, etc.
