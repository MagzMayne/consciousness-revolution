# Nixpacks Configuration
# This is a static HTML/CSS/JS site with Node.js for serving
# Forces Nixpacks to treat this as a Node.js project

[phases.setup]
nixPkgs = ["nodejs", "npm"]

[phases.install]
cmds = ["npm ci --legacy-peer-deps || npm install --legacy-peer-deps"]

[start]
cmd = "npm start"
