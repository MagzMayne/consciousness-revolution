# ARCHITECTURE_1_TRINITY_7DOMAIN_PLATFORM_v1
## C2 ARCHITECT - System Design
## Location: 100X_DEPLOYMENT/
## Status: ACTIVE
## Created: 2026-02-28 | Updated: 2026-02-28
## Connects: Supabase, R2, Stripe Connect
## Next: Deploy database schema

---

# 7-DOMAIN PLATFORM - ARCHITECTURE BLUEPRINT

## DOMAIN ROUTING

```
User: darrick_preble
├── command.darrick-preble.platform.io   (1_COMMAND)
├── build.darrick-preble.platform.io     (2_BUILD)
├── connect.darrick-preble.platform.io   (3_CONNECT)
├── protect.darrick-preble.platform.io   (4_PROTECT)
├── grow.darrick-preble.platform.io      (5_GROW)
├── learn.darrick-preble.platform.io     (6_LEARN)
└── transcend.darrick-preble.platform.io (7_TRANSCEND)
```

**Custom Domain Support:**
```
command.mysite.com → CNAME → command.darrick-preble.platform.io
```

---

## DATABASE SCHEMA (Core Tables)

```sql
-- USERS
CREATE TABLE users (
  id UUID PRIMARY KEY,
  username VARCHAR(50) UNIQUE,
  email VARCHAR(255) UNIQUE,
  stripe_customer_id VARCHAR(100),
  stripe_connect_account_id VARCHAR(100),
  plan_tier VARCHAR(20) DEFAULT 'free',
  storage_used_bytes BIGINT DEFAULT 0,
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- DOMAINS (7 per user)
CREATE TABLE user_domains (
  id UUID PRIMARY KEY,
  user_id UUID REFERENCES users(id),
  domain_number INT CHECK (domain_number BETWEEN 1 AND 7),
  domain_name VARCHAR(20), -- command, build, connect, etc.
  custom_domain VARCHAR(255),
  is_public BOOLEAN DEFAULT TRUE,
  UNIQUE(user_id, domain_number)
);

-- CONTENT (AI or user-created)
CREATE TABLE content (
  id UUID PRIMARY KEY,
  user_id UUID REFERENCES users(id),
  domain_id UUID REFERENCES user_domains(id),
  content_type VARCHAR(50), -- song, book, shirt, code
  title VARCHAR(255),
  ai_generated BOOLEAN DEFAULT FALSE,
  file_path TEXT,
  is_for_sale BOOLEAN DEFAULT FALSE,
  price_usd DECIMAL(10, 2),
  created_at TIMESTAMPTZ DEFAULT NOW()
);

-- TRANSACTIONS
CREATE TABLE transactions (
  id UUID PRIMARY KEY,
  buyer_user_id UUID REFERENCES users(id),
  seller_user_id UUID REFERENCES users(id),
  content_id UUID REFERENCES content(id),
  amount_usd DECIMAL(10, 2),
  platform_fee_usd DECIMAL(10, 2), -- 10%
  seller_payout_usd DECIMAL(10, 2), -- 90%
  status VARCHAR(20),
  created_at TIMESTAMPTZ DEFAULT NOW()
);
```

---

## TECH STACK

| Layer | Choice |
|-------|--------|
| Frontend | Next.js 14 + Tailwind |
| Backend | Bun + Hono |
| Database | Supabase Postgres |
| Storage | Cloudflare R2 |
| Queue | BullMQ + Upstash Redis |
| AI | Claude + DALL-E + Suno |
| Commerce | Stripe Connect |
| CDN | Cloudflare |

---

## REPLACES

| Service | Our Platform |
|---------|--------------|
| GitHub | Code hosting per domain |
| Netlify | Site deployment |
| S3 | File storage (R2) |
| Shopify | Built-in commerce |
| Railway | Backend hosting |

---

## SCALING

| Users | Cost/Month | Revenue |
|-------|------------|---------|
| 1,000 | $1,250 | $10,000 |
| 10,000 | $12,500 | $100,000 |
| 100,000 | $125,000 | $1,000,000 |

---

## SYSTEM DIAGRAM

```
┌─────────────────────────────────┐
│         EDGE (Cloudflare)       │
│  {domain}.{user}.platform.io    │
└────────────┬────────────────────┘
             │
┌────────────▼────────────────────┐
│         API (Bun + Hono)        │
│  User • Content • Commerce      │
└────────────┬────────────────────┘
             │
┌────────────▼────────────────────┐
│       DATABASE (Supabase)       │
│  Users • Domains • Content      │
└────────────┬────────────────────┘
             │
┌────────────▼────────────────────┐
│        STORAGE (R2)             │
│  Files • Deployments • Media    │
└─────────────────────────────────┘
```

---

*Generated by Trinity Protocol: C2 ARCHITECT*
